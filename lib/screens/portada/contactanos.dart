import 'package:flutter/material.dart';


class Contactanos extends StatelessWidget {
  const Contactanos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Contactanos",
      style: TextStyle(
        fontSize: 100,
      ),),
    );
  }
}