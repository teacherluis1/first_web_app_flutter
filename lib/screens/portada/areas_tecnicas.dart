import 'package:flutter/material.dart';


class Areastecnicas extends StatelessWidget {
  const Areastecnicas({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Areas Tecnicas",
      style: TextStyle(
        fontSize: 100,
      ),),
    );
  }
}